# Attention-Probe-RAG Dependencies
# 版本约束已放宽，服务器自带 torch 2.7.0 + Python 3.12.3
# 去掉 torch 行避免覆盖已有安装，让 pip 自动解析兼容版本
transformers
accelerate
datasets
scikit-learn
numpy
matplotlib
seaborn
sentence-transformers
tqdm
safetensors
